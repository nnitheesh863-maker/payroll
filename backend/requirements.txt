# ── PeoplePay360 Phase 0 Dependencies ───────────────────────────
# Backend — Flask
Flask>=3.1.0
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.1.0

# Validation / Config
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# Database
SQLAlchemy>=2.0.30
psycopg[binary]>=3.2.0
alembic>=1.13.0

# Phase 7 — PDF generation
reportlab>=4.0.0

# Phase 8 — JWT authentication
PyJWT>=2.8.0

# Testing
pytest>=8.0.0
